import React, { useState, useEffect, useCallback } from 'react';
import { NativeBaseProvider, Text, Box, Row, Column, Center } from 'native-base';

const AdminStaffPanel = ({ navigation }) => {
    return (
        <NativeBaseProvider>
            <Text>AdminStaffPanel</Text>
        </NativeBaseProvider>
    )
};
export default AdminStaffPanel;